name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Lint & Format
  # ---------------------------------------------------------------------------
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Uncomment and configure for your language/tooling

      # --- Node.js / TypeScript ---
      # - uses: oven-sh/setup-bun@v2
      #   with:
      #     bun-version: latest
      # - run: bun install --frozen-lockfile
      # - run: bun lint
      # - run: bun typecheck

      # --- Python ---
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - run: pip install ruff mypy
      # - run: ruff check .
      # - run: ruff format --check .
      # - run: mypy .

      # --- Go ---
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.23'
      # - uses: golangci/golangci-lint-action@v6

      - run: echo "TODO: configure lint step for your stack"

  # ---------------------------------------------------------------------------
  # Tests
  # ---------------------------------------------------------------------------
  test:
    name: Test
    runs-on: ubuntu-latest

    # TODO: Add service containers if needed
    # services:
    #   postgres:
    #     image: postgres:16
    #     env:
    #       POSTGRES_USER: test
    #       POSTGRES_PASSWORD: test
    #       POSTGRES_DB: testdb
    #     ports:
    #       - 5432:5432
    #     options: >-
    #       --health-cmd pg_isready
    #       --health-interval 10s
    #       --health-timeout 5s
    #       --health-retries 5

    steps:
      - uses: actions/checkout@v4

      # TODO: Uncomment and configure for your language/tooling

      # --- Node.js / TypeScript ---
      # - uses: oven-sh/setup-bun@v2
      # - run: bun install --frozen-lockfile
      # - run: bun test --coverage
      # - uses: codecov/codecov-action@v4
      #   with:
      #     token: ${{ secrets.CODECOV_TOKEN }}

      # --- Python ---
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - run: pip install -e ".[dev]"
      # - run: pytest --cov --cov-report=xml
      # - uses: codecov/codecov-action@v4

      # --- Go ---
      # - uses: actions/setup-go@v5
      # - run: go test -race -coverprofile=coverage.out ./...
      # - uses: codecov/codecov-action@v4

      - run: echo "TODO: configure test step for your stack"

    env:
      # TODO: Add required environment variables
      NODE_ENV: test
      # DATABASE_URL: postgresql://test:test@localhost:5432/testdb

  # ---------------------------------------------------------------------------
  # Security Audit
  # ---------------------------------------------------------------------------
  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Uncomment for your language

      # --- Node.js ---
      # - uses: oven-sh/setup-bun@v2
      # - run: bun install --frozen-lockfile
      # - run: bun audit

      # --- Python ---
      # - run: pip install pip-audit
      # - run: pip-audit

      # --- Go ---
      # - uses: actions/setup-go@v5
      # - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      # - run: govulncheck ./...

      - run: echo "TODO: configure security audit for your stack"

  # ---------------------------------------------------------------------------
  # Build (optional — for compiled languages or bundled apps)
  # ---------------------------------------------------------------------------
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      # TODO: Uncomment and configure build step

      # --- Node.js ---
      # - uses: oven-sh/setup-bun@v2
      # - run: bun install --frozen-lockfile
      # - run: bun build

      # --- Go ---
      # - uses: actions/setup-go@v5
      # - run: go build ./...

      - run: echo "TODO: configure build step for your stack"
